export class Project {

  constructor(public title: string, public category: string, public description: string, public difficulty: string, public numberOfSteps: number, public steps: string[]) {

  }
}